{
  "name": "@interview-prep/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "preevaluate": "npm run build --workspace @interview-prep/core && tsc -p tsconfig.json",
    "evaluate": "node dist/evaluate.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@interview-prep/core": "0.1.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
